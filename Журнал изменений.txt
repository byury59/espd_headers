=== Журнал изменений проекта espd_headers (ранее PPU_SHAPS) ===

Формат записи:
ДДММГГГГ-N (N — номер изменения за день) - XXXX (сквозной номер версии)
добавили ...
изменили ...
убрали ...

Сюда пишутся только фактические изменения самого espd_headers (код, логика, файлы программы).
Тестирование, отладка, отчёты и планы на будущее сюда не входят.

--------------------------------------------------

22092026-1 - 0001
создали структуру проекта espd_headers
добавили исходную версию PPU_SHAPS.py в legacy
добавили анализ исходной версии docs/Анализ_PPU_SHAPS.txt
добавили контрольный файл tests/fixtures/legacy_defects.c

22092026-2 - 0002
разбили PPU_SHAPS.py на модули src/espd_headers без изменения поведения
добавили точку входа src/main.py
добавили build_exe.bat и commit.bat
добавили тест совпадения результата с legacy tests/test_legacy_equivalence.py
добавили Инструкция.txt

22092026-3 - 0003
переименовали CHANGELOG.txt в «Журнал изменений.txt»
добавили шаблоны шапок templates/header.txt и templates/header_func.txt

24092026-1 - 0004
изменили работу для пользователя по образцу espd_parser: комплект кладётся в папку внутри проекта, по умолчанию обрабатывается папка на уровень выше программы
добавили параметры запуска --path, --out, --yes, --version, --help (короткая запись <путь> [<папка_результатов>] тоже работает)
добавили подтверждение перед изменением файлов и ожидание Enter при запуске двойным щелчком
добавили папку результатов на каждый запуск: Результаты\ДДММГГ-N_ignore, в ней журнал espd_headers_run.log и отчёт espd_headers_Шапки.txt
убрали loaded_files.txt в текущей папке: его заменил отчёт в папке запуска
изменили исключения папок: теперь из src\ignore.txt (как в espd_parser), а не из списка в коде
изменили чтение шаблонов: из src\templates рядом с программой, а не из текущей папки
убрали ограничение 34 строки на шаблон шапки модуля
изменили порядок обработки файлов: по пути, одинаковый на любом компьютере
перенесли шаблоны шапок в src\templates
добавили build.py: exe с версией в имени espd_headers_XXXX.exe, комплект в dist\espd_headers_XXXX
перенесли build_exe.bat и commit.bat в папку scripts, батники работают из корня репозитория
заменили commit.bat на общий из espd_parser: спрашивает ветку и сообщение
добавили scripts\run_checks.bat: проверка на копии TestProject, два прогона подряд, лог в checks\checks_log.txt
добавили TestProject: тестовый проект на C
изменили __version__: в одинарных кавычках, как в espd_parser
убрали requirements.txt: список библиотек в Инструкция.txt и README.md
изменили .gitignore: как в espd_parser
переписали README.md и Инструкция.txt по образцу espd_parser
